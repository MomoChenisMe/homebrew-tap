# Speclink Homebrew Tap

[Speclink](https://github.com/MomoChenisMe/speclink) 的 Homebrew tap。

```sh
brew install MomoChenisMe/tap/speclink
```

`Formula/speclink.rb` 由 release 管線自動更新（每個 release tag 對應一版）。
